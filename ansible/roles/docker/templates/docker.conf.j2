[Service]
ExecStart=
ExecStart=/usr/bin/dockerd -H fd://{% if 'docker_registry' in groups and inventory_hostname != groups['docker_registry'] | first %} --registry-mirror http://{{ groups['docker_registry'] | first }}:5000 --insecure-registry {{ groups['docker_registry'] | first }}:5000{% endif %}{% if 'consul_servers' in groups %} --dns {{ private_ip | ipaddr('address') }}{% endif %}

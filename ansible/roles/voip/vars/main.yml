video_servers: "{{ 'video_servers' if enable_jvb_cluster is defined and enable_jvb_cluster | lower == 'true' and groups['video_servers'] is defined else 'backend_servers' if enable_jvb_cluster is defined and enable_jvb_cluster | lower == 'true' and groups['video_servers'] is not defined else 'voip_servers' }}"
video_servers_count: "{{ groups['video_servers'] | count if groups['video_servers'] is defined and enable_jvb_cluster is defined and enable_jvb_cluster | lower == 'true' else groups['backend_servers'] | count if enable_jvb_cluster is defined and enable_jvb_cluster | lower == 'true' and groups['video_servers'] is not defined else 1 }}"
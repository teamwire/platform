object User "admin" {
  import "generic-user"

  display_name = "Icinga 2 Admin"
  groups = [ "icingaadmins" ]

  email = "{{ monitoring_email_addresses | list | join(", ") }}"
}

object UserGroup "icingaadmins" {
  display_name = "Icinga 2 Admin Group"
}

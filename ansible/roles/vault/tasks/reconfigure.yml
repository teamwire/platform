---
- name: Write Vault config
  ansible.builtin.template:
    src: vault.hcl.j2
    dest: /etc/vault/vault.hcl
    owner: vault
    group: vault
    mode: 0644
  notify:
    - Restart Vault to apply new configuration
    - Unseal Vault
    - Wait until Vault is unsealed and available

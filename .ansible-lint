---
exclude_paths:
  # This file has issues with the schema that I'm not able to figure out what
  - ansible/playbooks/procedures/galera-bootstrap.yml

#warn_list:
#   - fqcn-builtins 
skip_list:
   - fqcn-builtins 
   - no-changed-when
   - var-naming
   - empty-string-compare
   - risky-file-permissions
